name: Process OpenAI.list

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 13 * * *'  # 每天下午1点执行

jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Download file
        run: |
          curl https://raw.githubusercontent.com/EAlyce/conf/main/Rule/OpenAI.list -o OpenAI.list

      - name: Process file
        run: |
          sed -i 's/, /,/g' OpenAI.list  # 去除逗号后的空格
          sort -u -o OpenAI.list OpenAI.list  # 去重

      - name: Commit changes
        run: |
          git config --global user.email "msosjaahwwj@gmail.com"
          git config --global user.name "EAlyce"
          git add OpenAI.list
          git commit -m "Remove comma spaces and deduplicate in OpenAI.list"
          git push
